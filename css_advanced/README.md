# alx_html_css — css_advanced

This project is a continuation of the HTML Advanced project, focusing on applying CSS to recreate the Figma design.

### 🔹 Project Requirements
- Recreate the final Figma page using CSS.
- Download and use all images provided in the project assets.
- Apply appropriate styling (colors, spacing, fonts) according to the Figma file.
- Update the HTML to include images, classes, and structure needed for styling.

### 🔹 Files in this Directory
- `README.md` — Documentation for the project.
- `index.html` — Contains the HTML markup from the HTML advanced task.
- `css/` folder — Will contain your stylesheet (`style.css`).
- `assets/` folder — Will contain images and fonts exported from Figma.

### 🔹 How to Run the Project
1. Clone the repository  
2. Navigate to the `css_advanced` directory  
3. Open `index.html` in your browser  

### 🔹 Figma File
This project uses a Figma design.  
Duplicate it to your Drafts to access specs such as spacing, fonts, and colors.

---

Feel free to modify this README as needed to match your final implementation.
